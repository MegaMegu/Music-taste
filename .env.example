# Example environment variables for Music-taste
# Copy this to .env (or .env.local) and fill in real values. Do NOT commit secrets.

# Vite client-side variables (exposed to the browser via import.meta.env).
# Set your Spotify app client ID and redirect URI (redirect must match settings in your Spotify Developer app).
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
VITE_SPOTIFY_REDIRECT_URI=http://localhost:5173/spotify/callback

# Server-side variables (used by the API route). Keep these secret and set them in your deployment provider (Vercel, Netlify, etc.)
# Upstash Redis REST URL and token (example provider used in api/incrementDownload.js)
UPSTASH_REDIS_REST_URL=https://<your-upstash-id>.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here
